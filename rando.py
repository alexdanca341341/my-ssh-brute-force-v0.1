import os
import random
first_number = random.randint(1, 255)
second_number = random.randint(0, 255)
print("-thread is 900")
os.system(f"python unknown.py -ip_class {first_number}.{second_number}. -thread 900")